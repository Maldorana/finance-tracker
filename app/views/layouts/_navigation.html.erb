<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="navbar-brand nav-link active" href="<%= root_path %>">Finance Tracker</a>
                </li>
                <li class="nav-item">
                     <a class="nav-link <%= 'active' if request.path == my_portfolio_path %>" aria-current="page" href="<%= my_portfolio_path %>">My portfolio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= 'active' if request.path == my_friends_path %>" href="<%= my_friends_path %>">My friends</a>
                </li>
              </ul>
              <form class="d-flex">
                <% if user_signed_in? %>
                  <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                      <a class="nav-link dropdown-toggle active" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <%= fa_icon 'user' %><%= current_user.username != nil ? current_user.username : "Anonymous" %>
                      </a>
                      <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><%= link_to "Edit profile", edit_user_registration_path, class: "dropdown-item" %></li>
                        <li><a class="dropdown-item" href="#">Another action</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="#">Something else here</a></li>
                      </ul>
                    </li>
                    <li class="nav-item">
                      <%= link_to 'Sign out', destroy_user_session_path, class: "nav-link", method: :delete %>
                    </li>
                  </ul>
                  <% else %>
                  <ul class="navbar-nav">
                    <li class="nav-item">
                      <%= link_to 'Log in', new_session_path(resource_name), class: "nav-link" %>
                    </li>
                  </ul>
                  <%= link_to 'Sign up', new_registration_path(resource_name), class: "btn btn-outline-primary ms-2" %>
                <% end %>
              </form>
            </div>
        </div>
  </div>
</nav>